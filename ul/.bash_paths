#!/bin/bash

# load .bash_vars first
[ -r "$HOME/.bash_vars" ] && . "$HOME/.bash_vars"

# chain basis PREFIX/opt/{run} paths
[ -n "${OPT_RUN}" ] && PATH=$PATH:$OPT_RUN/bin:$OPT_RUN/sbin

# java home
[ -n "$JAVA_HOME" ] && PATH=$PATH:$JAVA_HOME/bin

# other paths

