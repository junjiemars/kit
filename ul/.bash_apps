#!/bin/bash

function exist_p() {
    local _p=$1
    which ${_p} &>/dev/null; echo $?
}

p_java=$(exist_p 'java')
if [[ 0 -eq p_java ]]; then
    export JAVA_HOME=$(readlink -f `type -p javac` | sed 's:/bin/javac::')
fi
